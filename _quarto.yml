project:
  type: manuscript

manuscript:
  article: index.qmd

format:
  html:
    comments:
      hypothesis: true
  # docx: default
  jats: default

  # (other formats)
  # pdf: default

execute:
  freeze: true

lang: "pt-BR"

crossref:
  fig-prefix: Figura   # (default is "Figure")
  tbl-prefix: Tabela   # (default is "Table")
